import React from "react";
import {Layout} from "antd";

const {Footer} = Layout;

export default function () {
    return (
        <Layout>
            <Footer>
                Footer
            </Footer>
        </Layout>
    )
}